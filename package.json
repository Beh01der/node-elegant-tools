{
  "name": "elegant-tools",
  "version": "1.0.0",
  "description": "",
  "module": "dist/index.esm.js",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "LOG_LEVEL=${LOG_LEVEL:=off} jest",
    "test-dev": "LOG_LEVEL=${LOG_LEVEL:=off} jest --watch",
    "bump": "npm version patch && git push --tags && git push --all",
    "release": "npm i && npm run bundle && npm run cleanup",
    "format": "dprint fmt ./src/**/*"
  },
  "keywords": [],
  "author": "Andrey Chausenko",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "@types/node": "^18.16.17",
    "esbuild": "^0.18.0",
    "jest": "^29.5.0",
    "npm-dts": "^1.3.12",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "collectCoverageFrom": ["src/**/*.ts"]
  }
}
